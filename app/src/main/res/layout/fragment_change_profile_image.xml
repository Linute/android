<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <com.mikhaellopez.circularimageview.CircularImageView
        android:id="@+id/changeprofileimage_image"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="32dp"
        android:scaleType="centerCrop"
        android:layout_marginBottom="@dimen/activity_vertical_margin"
        app:border_color="@color/grey_color"
        app:border_width="1dp"/>

    <RelativeLayout android:id="@+id/changeprofileimage_buttons"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_below="@id/changeprofileimage_image"
        >

        <TextView android:id="@+id/changeprofileimage_change_button"
                  android:layout_width="wrap_content"
                  android:layout_height="wrap_content"
                  android:gravity="center"
                  android:padding="20dp"
                  android:layout_centerHorizontal="true"
                  android:textSize="@dimen/header_font_size"
                  android:text="Change"/>

        <TextView android:id="@+id/changeprofileimage_save_button"
                  android:layout_width="wrap_content"
                  android:layout_height="wrap_content"
                  android:gravity="center"
                  android:layout_centerHorizontal="true"
                  android:layout_below="@id/changeprofileimage_change_button"
                  android:padding="20dp"
                  android:textSize="@dimen/header_font_size"
                  android:text="Save"/>
    </RelativeLayout>

    <ProgressBar
        android:id="@+id/changeprofileimage_progressbar"
        style="?android:attr/progressBarStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/changeprofileimage_image"
        android:layout_marginTop="32dp"
        android:layout_centerHorizontal="true"
        android:visibility="gone"
        />

</RelativeLayout>
